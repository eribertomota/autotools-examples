TESTS_ENVIRONMENT = \
    mscompress_abspath='$(abs_top_builddir)'; \
    export mscompress_abspath;

TESTS = test.sh

EXTRA_DIST = $(TESTS)

EXTRA_DIST += \
    test.compressed \
    test.txt

CLEANFILES = \
    test.new \
    test.txt_
