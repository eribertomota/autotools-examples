SUBDIRS = fritzbox callmonitor areacodes_global reverselookup webjournal

if WITH_PULSEAUDIO
SUBDIRS += pulseaudio
endif

if WITH_PORTAUDIO
SUBDIRS += portaudio
endif

if WITH_GSTREAMER1
SUBDIRS += gstreamer1
endif

if WITH_SECRET
SUBDIRS += secret
endif

if WIN32
SUBDIRS += wincred
endif
